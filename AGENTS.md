We are working on the POC in `mpm-DINO-poc/`.

First, read:

- `mpm-DINO-poc/CONTEXT.md`
- `mpm-DINO-poc/V2_CONTEXT.md`

For V2 implementation, then read `docs/v1/CONCLUSION.md`,
`docs/v2/CONTEXT.md`, and `docs/v2/PLAN.md`. Treat all relative paths in these
documents as relative to `mpm-DINO-poc/`.

Treat all Markdown links and local relative paths in CONTEXT.md as relative to the directory containing CONTEXT.md. Inspect referenced papers, repositories, or datasets selectively when relevant to the task.
